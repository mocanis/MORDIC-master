<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    >

    <!--<com.chris.mordic.ui.HeaderViewPager
        android:id="@+id/HeaderViewPager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/white"
        android:orientation="vertical"
        app:hvp_topOffset="0dp">

        <include layout="@layout/layout_show_words_num"/>

        <ListView
            android:id="@+id/listView_wordbook"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#ffffff">
        </ListView>


    </com.chris.mordic.ui.HeaderViewPager>-->
    <ListView
        android:id="@+id/listView_wordbook"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
    </ListView>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:layout_alignParentBottom="true"
        android:background="#efefef">

        <TextView
            style="@style/tab_style"
            android:text="提醒"
            />

        <TextView
            style="@style/tab_style"
            android:text="创建生词本"

            />

        <TextView
            android:id="@+id/tv_importWord"
            style="@style/tab_style"
            android:text="导入生词"

            />
    </LinearLayout>
    <!--
        <LinearLayout
            android:id="@+id/ll_improtWordList"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingTop="30dp">

            <Button
                android:id="@+id/button_selectFile"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="选择本地生词本:"/>

            <TextView
                android:id="@+id/text_selectedfile"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="20sp"/>
        </LinearLayout>

        <Button
            android:id="@+id/button_importNewWords"
            android:visibility="gone"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="right"
            android:text="开始导入"/>-->

</RelativeLayout>
